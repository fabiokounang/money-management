<!DOCTYPE html>
<html lang="id">
<head>
  <%- include('../partials/app_head') %>
</head>
<body class="min-h-screen bg-stone-50 text-stone-800">
  <%- include('../partials/sidebar') %>
  <div id="app-overlay" class="app-overlay lg:hidden"></div>

  <main class="app-main px-4 py-4 sm:px-6 sm:py-6">
    <%- include('../partials/topbar') %>

    <section class="mt-6 overflow-hidden rounded-[32px] border border-amber-200/80 bg-amber-50/40 shadow-xl shadow-stone-200/50">
      <div class="border-b border-amber-100 bg-gradient-to-r from-amber-50 via-white to-stone-50 px-5 py-5 sm:px-7 sm:py-6">
        <h1 class="text-2xl font-bold tracking-tight text-stone-900 sm:text-3xl">Database perlu migrasi</h1>
        <p class="mt-2 text-sm text-stone-700">Fitur jadwal berulang membutuhkan tabel <code class="rounded bg-white px-1.5 py-0.5 text-xs">recurring_schedules</code> di MySQL.</p>
      </div>

      <div class="space-y-4 px-5 py-6 sm:px-7 sm:py-7 text-sm leading-relaxed text-stone-700">
        <p>Jalankan skrip SQL berikut sekali pada database production (phpMyAdmin, MySQL client, atau panel hosting):</p>
        <pre class="overflow-x-auto rounded-2xl border border-stone-200 bg-stone-900 p-4 text-xs text-stone-100"><code>-- File di repo: sql/migration_recurring_schedules.sql
-- Salin isi file itu dan jalankan di MySQL.</code></pre>
        <p>Setelah tabel terbentuk, muat ulang halaman <a href="/recurring" class="font-semibold text-emerald-700 underline">/recurring</a>.</p>
        <a href="/dashboard" class="inline-flex rounded-2xl border border-stone-200 bg-white px-5 py-2.5 text-sm font-semibold text-stone-800 shadow-sm hover:border-stone-300">Ke dashboard</a>
      </div>
    </section>
  </main>
</body>
</html>
